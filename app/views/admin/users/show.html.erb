<div class="row">
  <% @users.each do |user| %>
    <div class="col-md-6 mb-4">
      <div class="card">
        <div class="card mb-4">
          <div class="card-body">
            <h1 class="card-title"><%= user.name %></h1>
          </div>
        </div>
      </div>
    </div>
  <% end %>
</div>